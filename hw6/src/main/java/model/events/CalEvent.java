package model.events;

/**
 * An abstract interface representing a calendar event's most basic
 * abilities that it should be able to carry out.
 */
public interface CalEvent extends Comparable<CalEvent> {
}
